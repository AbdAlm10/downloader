# Netlify = واجهة الدخول فقط — التحليل والتحميل يعملان على Render (Docker + yt-dlp).
# عند إضافة Railway لاحقاً: غيّر الرابط أدناه إلى رابط Railway.

[[redirects]]
  from = "/*"
  to = "https://almonzel.onrender.com/:splat"
  status = 302
  force = true
