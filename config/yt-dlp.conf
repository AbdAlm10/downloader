# Applied on Docker (/etc/yt-dlp.conf) and locally (config/yt-dlp.conf).
# JS runtimes are injected by src/lib/ytdlp.ts for the current OS.
# Refresh EJS solvers when YouTube changes challenges (safe with official yt-dlp_linux binary).
--remote-components
ejs:github
